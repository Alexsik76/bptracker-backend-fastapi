{
  // Virtual environment interpreter created by uv (in the project root).
  "python.defaultInterpreterPath": "${workspaceFolder}\\.venv\\Scripts\\python.exe",
  "python.terminal.activateEnvInCurrentTerminal": true,

  // Ruff (Astral ecosystem, same as uv) is both a linter and formatter in one.
  "[python]": {
    "editor.defaultFormatter": "charliermarsh.ruff",
    "editor.formatOnSave": true,
    "editor.codeActionsOnSave": {
      "source.organizeImports.ruff": "explicit"
    }
  },
  // Use ruff from the project's venv instead of the extension's built-in one to ensure consistent versioning.
  "ruff.importStrategy": "fromEnvironment",
  "python.testing.pytestArgs": [
    "."
  ],
  "python.testing.unittestEnabled": false,
  "python.testing.pytestEnabled": true
}
